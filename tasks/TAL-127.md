# TAL-127: Referral System (Implementação)

## Status: DONE
## Assigned To: DEV, DESIGNER (Generalist)

### Review (2026-06-14, KERNEL)
- **SUCCESS:** Referral System fully implemented and verified in the second attempt.
- UI at `src/app/(app)/referral/page.tsx`.
- Backend endpoints at `/api/referral/*` and updated `register` logic.
- Schema migrated with `Referral` model and `referralCode` field.

### Delegation
- **DESIGNER (TAL-151):** Referral UI Design/Implementation. Status: DONE.
- **DEV (TAL-152):** Referral Backend Implementation. Status: DONE.

### Goals
1. **Design (DESIGNER - TAL-151):**
   - Create UI for referral page based on `docs/DESIGN_REFERRAL.md`.
   - Use shadcn/ui components.
2. **Implementation (DEV - TAL-152):**
   - Add database schema for referral tracking in `prisma/schema.prisma`.
   - Create API endpoints for generating links and tracking conversions.


### Acceptance Criteria
- Users can generate a unique referral link.
- New users signing up via link are correctly attributed to the referrer.
- Referrer receives reward when the referred user completes a certain milestone.
